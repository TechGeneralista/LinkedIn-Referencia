<UserControl x:Class="UCVisionResultExplorerApp.ResultExplorerSourceV"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UCVisionResultExplorerApp"
             mc:Ignorable="d" d:DesignWidth="500">
    <Grid Grid.Column="0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Button Grid.Column="0" Content="{Binding LanguageDC.Browse.Value, FallbackValue=Browse}" Click="BrowseButtonClick" Margin="5" Padding="5" VerticalAlignment="Center"/>
        <TextBlock Grid.Column="1" Text="{Binding LanguageDC.DataSourceColon.Value,FallbackValue=DataSource:}" Margin="5,5,0,5" VerticalAlignment="Center"/>
        <TextBlock Grid.Column="2" Text="{Binding SourcePath.Value, FallbackValue=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx}" Margin="5" VerticalAlignment="Center"/>

    </Grid>
</UserControl>
