using System.Collections.Generic;


namespace Common
{
    //public static class ObjectContainer
    //{
    //    static Dictionary<string, object> objects = new Dictionary<string, object>();
    //    public static void Set<T>(T obj, string id = null) => objects[string.Format("{0}-{1}", typeof(T).Name, id)] = obj;
    //    public static T Get<T>(string id = null)
    //    {
    //        try
    //        {
    //            return (T)objects[string.Format("{0}-{1}", typeof(T).Name, id)];
    //        }
    //        catch { }

    //        return default;
    //    }
    //}
}
